import React from "react";
import ReactDOM from 'react-dom/client';
import TextEditor from "./frontend";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <TextEditor/>
);
